version: '3.8.1'
services:
  web:
    build:
      context: ./search-engine
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    depends_on:
      - server
  server:
    build:
      context: ./server
      dockerfile: Dockerfile

  dev:
    build:
      context: ./dev
      dockerfile: Dockerfile
